# skywave_surfer configuration - copy to `.env` and edit, or leave absent
# to use the defaults. Real environment variables override this file.

# Path to your kiwiclient checkout (default: ~/dev/kiwiclient)
#KIWICLIENT=~/dev/kiwiclient

# Where session recordings land (default: ~/skywave_recordings)
#OUTPUT_DIR=~/skywave_recordings
